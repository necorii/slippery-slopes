# Improved iOS Build Configuration

# Platform Flags
platform_flags: 
  -DPLATFORM_IOS

# Info.plist Generation
info_plist: 
  generate

# Copying Assets
assets: 
  copy:
    include:
      - fonts
      - images
      - other_assets
