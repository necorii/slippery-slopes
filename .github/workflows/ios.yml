name: iOS Build Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Clone Raylib
        run: |
          git clone --recursive https://github.com/Raylib/Raylib.git

      - name: Set up Xcode
        uses: maxheld83/setup-xcode@v1
        with:
          xcode-version: 'latest'

      - name: Build with Xcode
        run: |
          xcodebuild -project Raylib.xcodeproj -scheme Raylib -sdk iphoneos -configuration Release

      - name: Generate Info.plist
        run: |
          echo '<?xml version="1.0" encoding="UTF-8"?>\n<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">\n<plist version="1.0">\n<dict>\n  <key>CFBundleName</key>\n  <string>MyApp</string>\n  <key>CFBundleIdentifier</key>\n  <string>com.necorii.slippery-slopes</string>\n  <key>CFBundleVersion</key>\n  <string>1.0.0</string>\n  <key>CFBundleShortVersionString</key>\n  <string>1.0.0</string>\n</dict>\n</plist>' > Info.plist

      - name: Copy Assets
        run: |
          cp -R assets/*.png .
          cp -R assets/*.wav .
          cp -R assets/*.ttf .

      - name: Package IPA
        run: |
          xcodebuild -exportArchive -archivePath build/MyApp.xcarchive -exportPath build -exportOptionsPlist ExportOptions.plist

      - name: Upload IPA
        uses: actions/upload-artifact@v2
        with:
          name: MyApp.ipa
          path: build/MyApp.ipa