# This is an example of how to set up Homebrew's prebuilt Raylib.
# Using the prebuilt version can help avoid Objective-C header issues.

# Before this step, ensure Homebrew is installed:
# brew install raylib

- name: Install Raylib
  run: |
    brew install raylib --HEAD

# Other steps in your workflow follow...